# chat-bot-whats
# Instalar node.js para windows
# Extrair pasta do projeto em qualquer local
# Abra cmd direto na pasta do projeto
# digitar comando 'npm install'
# Executar comando 'npm run start:qrcode' para emitir o qrcode
# Caso apresente erro, execute o comando 'npm uninstall puppeteer" e execute o outro comando 'npm install puppeteer@latest'
# Tente executar o comando do qrcode novamente.
